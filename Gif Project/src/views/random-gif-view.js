// import { renderFavoriteStatus } from "../events/helpers.js";

/**
 * Converts a randomGif object into a random gif view HTML string.
 *
 * @param {Object} randomGif - The randomGif object.
 * @return {string} The HTML string representing the random gif view.
 */
// export const toRandomGifView = (randomGif) => `
//   <a href="#">
//     <img class="gif" id="${randomGif.id}" src="${randomGif.images.fixed_width.url}">
//     ${renderFavoriteStatus(randomGif.id)}
//   </a>
// `;
